local Algorithms = {}

local FirstLayer = require(script.FirstLayer)
Algorithms.FirstLayer = FirstLayer

local F2L = require(script.F2L)
Algorithms.F2L = F2L

local OLL = require(script.OLL)
Algorithms.OLL = OLL

local PLL = require(script.PLL)
Algorithms.PLL = require(script.PLL)

return Algorithms